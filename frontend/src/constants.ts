/**
 * 顯示容器尺寸限制
 * 用於 M 倍率計算 — 調整這裡會同時影響裁切模式與預覽模式的容器大小
 */
export const CONTAINER_MIN_WIDTH = 600;
export const CONTAINER_MAX_WIDTH = 800;
export const CONTAINER_MIN_HEIGHT = 600;
export const CONTAINER_MAX_HEIGHT = 800;
